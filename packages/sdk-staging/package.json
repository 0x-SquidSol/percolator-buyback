{
  "name": "@percolatorct/sdk-staging",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "description": "Verification-only toolchain for the buyback SDK staging. The integrator transfers src/ only — package.json, tsconfig.json, and lockfile do NOT cross over.",
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@solana/web3.js": "^1.95.4"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18"
  }
}
